# Penalized model-selection placeholder
#
# This module is intended to compute a penalized criterion and select the
# model dimension D_m that optimizes that criterion.
#
# Planned responsibilities:
# - combine the raw objective value with the penalty pen_n(D_m);
# - evaluate the penalized criterion across candidate model dimensions;
# - choose the selected D_m according to the chosen minimization or maximization rule;
# - return enough diagnostic information to compare raw and penalized curves.
#
# No implementation has been added yet.
